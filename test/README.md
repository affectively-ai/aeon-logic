# Tests

- Parent README: [../README.md](../README.md)

This directory validates:

- checker correctness and determinism
- checker wavefront and frontier-fill diagnostics
- superposition chain behavior (fork/interfere/measure)
- complex-amplitude superposition behavior
- temporal formula parsing/compilation (`eventually@q`, `until@q`)
- topology and flow bridge adapters
- generated superposition TLA/CFG artifact consistency
- TLC config parse/serialize compatibility
- TLA module parse/render compatibility
- TLA sandbox partitioning and parser round-trip execution reports
- Lean project discovery, metadata inspection, and optional build orchestration
- TLC trace conversion and parsing
- native `.gg` parsing/modeling/checking behavior
- inversion-pair checks, boundary sweep frontiers, and one-shot learning reports
